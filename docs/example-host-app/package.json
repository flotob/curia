{
  "name": "plugin-host-app",
  "version": "1.0.0",
  "description": "Sample host application for testing plugin embedding",
  "private": true,
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "clean": "rm -rf dist",
    "lint": "eslint src --ext .ts,.html"
  },
  "dependencies": {
    "@common-ground-dao/cg-plugin-lib": "workspace:*",
    "@common-ground-dao/cg-plugin-lib-host": "workspace:*"
  },
  "devDependencies": {
    "typescript": "^5.0.0",
    "vite": "^5.0.0",
    "@types/node": "^20.0.0"
  }
} 